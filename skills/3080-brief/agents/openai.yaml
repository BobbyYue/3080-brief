interface:
  display_name: "3080 Brief"
  short_description: "30-second judgment, 80% in one picture"
  brand_color: "#0055A4"
  default_prompt: "Use $3080-brief now. Start its resumable run, do not skip checkpoints, keep the source unchanged, and finish only when the generated link/path has a PASS delivery receipt or an exact blocker."

policy:
  allow_implicit_invocation: true
